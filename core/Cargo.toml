[package]
name = "ld-core"
version = "0.1.0"
edition.workspace = true

[dependencies]
axum = { workspace = true }
sqlx = { workspace = true, features = [ "runtime-tokio", "tls-rustls-ring-webpki", "postgres" ] }
